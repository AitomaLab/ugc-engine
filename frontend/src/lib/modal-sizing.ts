// Standard modal height/width caps. Tuned for 14" laptop usable viewport
// (~870px after browser chrome) while still scaling up on larger monitors.

export const MODAL_HEIGHT = 'min(82vh, 680px)';        // image/video edit modals
export const MODAL_HEIGHT_SHORT = 'min(82vh, 640px)';  // schedule, product
export const MODAL_WIDTH = 'min(94vw, 1080px)';
export const MODAL_WIDTH_NARROW = 'min(94vw, 900px)';
